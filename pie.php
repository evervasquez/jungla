			</div>
            <footer>
                <h6>
                	<div class="vcard">
						Copyright 2012 &copy; <a href="#" id="cr">La Jungla</a>
                    </div>
                </h6>
                <!--
                <script type="text/javascript">
					/*Usamos localStorage para guardar informacion permanente en el navegador cuando visita la pagina */
					if(localStorage.pageCount){
						localStorage.pageCount=Number(localStorage.pageCount)+1;
					}else{localStorage.pageCount = 1;}
					/*document.write("Has visitado mi pagina "+localStorage.pageCount+" veces");*/
					/*Usamos sessionStorage para guardar las sesiones en el navegador cuando visita la pagina */
					if(sessionStorage.pageCount){
						sessionStorage.pageCount=Number(sessionStorage.pageCount)+1;
					}else{sessionStorage.pageCount = 1;}
					/*document.write("En esta sesion has visitado mi pagina "+sessionStorage.pageCount+" veces");*/
				</script>
                -->
            </footer>
    	</div>
    </body>
</html>